package com.keren.solver;

public class Valid {
    private boolean valid;

    public Valid(boolean valid) {
        this.valid = valid;
    }

    public boolean isValid() {
        return valid;
    }
}
